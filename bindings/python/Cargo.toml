[package]
name = "synapse-python"
version = "0.1.0"
edition = "2021"

[lib]
name = "synapse"
crate-type = ["cdylib"]

[dependencies]
pyo3 = { version = "0.23", features = ["extension-module"] }
synapse-core = { path = "../../core" }
