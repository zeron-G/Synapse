[package]
name = "synapse-idl"
version = "0.1.0"
edition = "2021"
description = "IDL schema compiler for Synapse cross-language bridge"
license = "MIT"

[dependencies]
clap = { version = "4", features = ["derive"] }

[dev-dependencies]
