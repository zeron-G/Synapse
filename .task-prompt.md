You are working on the Synapse project - a cross-language runtime bridge via shared memory + lock-free ring buffers.

Your task: COMPREHENSIVE TEST COMPLETION for the existing codebase.

Current state:
- core/ has 7 unit tests + 4 integration tests + 1 doc test = 12 tests (all passing)
- idl/ has 32 unit tests + 30 integration tests = 62 tests (all passing)
- NO cross-process tests, NO error path tests, NO Python/C++ binding tests

You need to add the following tests:

## 1. Cross-Process Real Communication Tests (core/tests/)
Create `cross_process_test.rs`:
- Spawn a child process that connects to shm created by parent
- Test real bidirectional message passing between two OS processes
- Test session token matching across processes
- Test state machine transitions (Init -> Ready) across processes
- Use std::process::Command to spawn child, pass shm name via args

## 2. Error Path Tests (core/tests/)
Create `error_path_test.rs`:
- Connect to non-existent shm → ShmError
- Magic number mismatch → BadMagic
- Version mismatch → VersionMismatch  
- Data too large for slot → DataTooLarge
- Ring full behavior (push until full, verify error)
- Double host (two processes both try to host same name)

## 3. Python Binding End-to-End Test (examples/ or tests/)
Create `test_python_bridge.py`:
- Test SynapseBridge class from python_sender.py
- Create host, send data, recv data (using threading for both sides)
- Test that Python bridge matches Rust wire format exactly
- Test error cases (connect to non-existent, send too large data)

## 4. C++ Header Compilation Test
Add to CI workflow (.github/workflows/ci.yml):
- Compile cpp_receiver.cpp with g++ -std=c++17 on Linux
- Just compilation test (no runtime, since it needs a host)
- Add a minimal C++ test that creates and reads header structures

## 5. Update test-local.sh
- Add Python test step
- Add C++ compilation test step
- Print test summary at end

Rules:
- All Rust tests must pass on both Linux and Windows
- Use `cargo fmt` after writing code
- Use `cargo clippy -- -D warnings` to check
- Run all tests after writing to verify they pass
- Commit with message: "test: comprehensive test suite - cross-process, error paths, Python e2e, C++ compile"

When completely finished, run this command to notify me:
openclaw system event --text "Done: subtask_01 test suite complete - cross-process, error paths, Python e2e, C++ compile tests added" --mode now
